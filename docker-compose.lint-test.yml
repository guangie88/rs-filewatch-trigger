version: '2'
services:
  lint-test:
    build:
      context: .
      dockerfile: Dockerfile-lint-test
    command:
      - bash
      - '-c'
      - 'rustfmt --check src/**.rs
         && cargo clippy --all
         && cargo test --all'
